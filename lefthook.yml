pre-push:
  commands:
    packages-audit:
      run: yarn audit

pre-commit:
  commands:
    eslint:
      glob: "*.{js,ts}"
      run: yarn lint {staged_files}
    rollup-build:
      glob: "*.{js,ts}"
      run: yarn build && git add index.js
